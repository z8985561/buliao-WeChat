<loading wx:if="{{!show}}">加载中...</loading>

<!-- 定制UI部分 -->
<swiper class="imageContainer" bindchange="handleChange" previous-margin="50rpx" next-margin="50rpx" circular autoplay>
  <block wx:for="{{3}}" wx:for-item='spItem' wx:key="index">
    <swiper-item class="item">
      <image class="itemImg {{currentIndex == index ? 'active': ''}}" src="http://img2.y01.cn/images/4/2019/05/BZGG7W4qXqg4Q16xZo4Mj6aN64JXGB.png"></image>
    </swiper-item>
  </block>
</swiper>
<view style='margin:0 auto 15px;height:1px;width:710rpx;background:#eee;'></view>
<!-- 定制UI部分 -->
<!-- <view wx:if="{{show}}" style='bottom'>
  <view class="category-search">
    <view class="weui-flex">
      <view class="weui-flex__item category-search-form">
        <view class="flex-head-search" id="searchBar">
          <navigator class="weui-search-bar__form" hoverClass="none" openType="navigate" url="/pages/goods/index/index?fromsearch=1">
            <view class="weui-search-bar__box category-search-box">
              <view class="flex-icon-search">
                <i class="icox icox-search" style="color: #b4b4b4;"></i>
              </view>
              <view class="weui-search-bar__input flex-input">输入关键字进行搜索</view>
              <navigator class="weui-icon-clear" id="searchClear" url="javascript:"></navigator>
            </view>
          </navigator>
        </view>
      </view>
    </view>
  </view>
  <view class="category-content category-search-bar weui-flex">
    <view class="category-content-left" wx:if="{{set.level>1}}">
      <scroll-view class="scroll-view_H" scrollY="true" style="height: 100%;">
        <view bindtap="tabCategory" class="content-left-item {{selector==0?'active':''}}" data-child="{{recommands}}" data-id="0" data-src="{{set.advimg}}">推荐分类</view>
        <view bindtap="tabCategory" class="content-left-item {{selector==item.id?'active':''}}" data-child="{{item.child}}" data-id="{{item.id}}" data-src="{{item.advimg}}" data-url="{{item.advurl}}" wx:for="{{category}}" wx:for-index="idx">
          {{item.name}}
        </view>
      </scroll-view>
    </view>
    <view class="category-ceontet-right weui-flex__item">
      <scroll-view class="scroll-view_H" scrollY="true" style="height:100%;">
        <view class="content-right-item">
          <view class="" style="width:100%;display:block;margin-bottom:28rpx" wx:if="{{set.level>1&&advimg}}">
            <navigator class="swipe external" id="advurl" url="/pages/goods/index/index?cate={{selector}}">
              <image id="advimg" mode="widthFix" src="{{advimg}}"></image>
            </navigator>
          </view>
          <block wx:if="{{set.level==1}}">
            <view class="fui-title">所有分类</view>
            <view class="fui-icon-group col-3">
              <navigator class="fui-icon-col block" openType="navigate" url="/pages/goods/index/index">
                <view class="icon {{set.style==0?'radio':''}}">
                  <text class="icox icox-app"></text>
                </view>
                <view class="text">所有商品</view>
              </navigator>
              <navigator class="fui-icon-col" openType="navigate" url="/pages/goods/index/index?cate={{item.id}}" wx:for="{{category}}" wx:for-index="idx">
                <view class="icon {{set.style==0?'radio':''}}">
                  <image src="{{item.advimg}}"></image>
                </view>
                <view class="text">{{item.name}}</view>
              </navigator>
            </view>
          </block>
          <view class="fui-icon-group col-3" wx:if="{{set.level>1}}">
            <navigator class="fui-icon-col " openType="navigate" url="/pages/goods/index/index" wx:if="{{selector==0&&back==0}}">
              <view class="icon {{set.style==0?'radio':''}}">
                <text class="icox icox-app"></text>
              </view>
              <view class="text">所有商品</view>
            </navigator>
            <block wx:if="{{set.level==2}}">
              <navigator class="fui-icon-col" openType="navigate" url="/pages/goods/index/index?cate={{item.id}}" wx:if="{{set.level>=item.level}}" wx:for="{{child}}" wx:for-index="idx">
                <view class="icon {{set.style==0?'crrcle':''}}">
                  <image src="{{item.thumb||icons.nopic}}"></image>
                </view>
                <view class="text">{{item.name}}</view>
              </navigator>
            </block>
            <block wx:if="{{set.level==3}}">
              <block wx:if="{{set.show==0}}">
                <block wx:if="{{selector==0}}">
                  <navigator bindtap="backParent" class="fui-icon-col" data-id="{{selector}}" data-parent="{{parent}}" wx:if="{{back==1}}">
                    <view class="icon {{set.style==0?'radio':''}}">
                      <text class="icox icox-toleft"></text>
                    </view>
                    <view class="text">返回上一级</view>
                  </navigator>
                  <navigator bindtap="{{item.child.length>0?'cateChild':''}}" class="fui-icon-col" data-child="{{item.child}}" data-parent="{{child}}" openType="navigate" url="{{item.child.length>0?'':'/pages/goods/index/index?cate='+item.id}}" wx:for="{{child}}" wx:for-index="idx">
                    <view class="icon {{set.style==0?'radio':''}}">
                      <image src="{{item.thumb||icons.nopic}}"></image>
                    </view>
                    <view class="text">{{item.name}}</view>
                  </navigator>
                </block>
                <block wx:else>
                  <view style="overflow:hidden" wx:for="{{child}}" wx:for-index="idx">
                    <view style="font-size:28rpx"> {{item.name}}</view>
                    <navigator class="fui-icon-col str" url="/pages/goods/index/index?cate={{items.id}}" wx:for="{{item.child}}" wx:for-item="items">
                      <view class="icon {{set.style==0?'radio':''}}">
                        <image binderror="error" src="{{items.thumb||icons.nopic}}"></image>
                      </view>
                      <view class="text" style="line-height:50px">{{items.name}}</view>
                    </navigator>
                  </view>
                </block>
              </block>
              <block wx:else>
                <navigator bindtap="backParent" class="fui-icon-col" data-id="{{selector}}" data-parent="{{parent}}" wx:if="{{back==1}}">
                  <view class="icon {{set.style==0?'radio':''}}">
                    <text class="icox icox-toleft"></text>
                  </view>
                  <view class="text">返回上一级</view>
                </navigator>
                <navigator bindtap="{{item.child.length>0?'cateChild':''}}" class="fui-icon-col str" data-child="{{item.child}}" data-parent="{{child}}" url="{{item.child.length>0?'':'/pages/goods/index/index?cate='+item.id}}" wx:for="{{child}}" wx:for-index="idx">
                  <view class="icon {{set.style==0?'radio':''}}">
                    <image binderror="error" src="{{item.thumb||icons.nopic}}"></image>
                  </view>
                  <view class="text">{{item.name}}</view>
                </navigator>
              </block>
            </block>
          </view>
        </view>
      </scroll-view>
    </view>
  </view>
</view> -->
<include src="/pages/common/menu.wxml"></include>